{
  "name": "d-tunes",
  "version": "1.0.0",
  "description": "D'Tunes web music player with rule-based recommendations",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "node server.js",
    "build": "echo 'No build step required'",
    "lint": "node --check server.js lib/*.js src/*.js",
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
